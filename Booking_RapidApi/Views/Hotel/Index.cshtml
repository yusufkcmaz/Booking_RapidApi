@* @model Booking_RapidApi.Models.SimpleHotelSearchResultViewModel

@{
    ViewData["Title"] = "Index";
}



<h2>Hotel Search</h2>

<form method="post" action="@Url.Action("Index", "Hotel")">
    <div>
        <label for="city_name">City Name</label>
        <input type="text" id="city_name" name="city_name" required />
    </div>

    <div>
        <label for="ArrivalDate">Arrival Date</label>
        <input type="date" id="ArrivalDate" name="ArrivalDate" required />
    </div>

    <div>
        <label for="DepartureDate">Departure Date</label>
        <input type="date" id="DepartureDate" name="DepartureDate" required />
    </div>

    <div>
        <label for="AdultCount">Adult Count</label>
        <input type="number" id="AdultCount" name="AdultCount" min="1" value="1" required />
    </div>

    <div>
        <label for="Currency">Currency</label>
        <input type="text" id="Currency" name="Currency" value="USD" required />
    </div>

    <button type="submit">Search Hotels</button>
</form>

 *@


        @model SearchHotelViewModel.Rootobject

@{
    ViewData["Title"] = "Hotel Search";
}

<h2>Hotel Search</h2>

<form method="post" action="@Url.Action("Index", "Hotel")">
    <div>
        <label for="city_name">City Name</label>
        <input type="text" id="city_name" name="city_name" required />
    </div>

    <div>
        <label for="ArrivalDate">Arrival Date</label>
        <input type="date" id="ArrivalDate" name="ArrivalDate" required />
    </div>

    <div>
        <label for="DepartureDate">Departure Date</label>
        <input type="date" id="DepartureDate" name="DepartureDate" required />
    </div>

    <div>
        <label for="AdultCount">Adult Count</label>
        <input type="number" id="AdultCount" name="AdultCount" min="1" value="1" required />
    </div>

    <div>
        <label for="Currency">Currency</label>
        <input type="text" id="Currency" name="Currency" value="USD" required />
    </div>

    <button type="submit">Search Hotels</button>
</form>

@if (Model?.data?.hotels != null)
{
    <h3>Hotel Results</h3>
    <table>
        <thead>
            <tr>
                <th>Hotel Name</th>
                <th>Price</th>
                <th>Review Score</th>
                <th>Image</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var hotel in Model.data.hotels)
            {
                <tr>
                    
                    <td>@hotel.property.name</td>
                    <td>@hotel.property.priceBreakdown</td>
                    <td>@hotel.property.reviewScore</td>
                    <td>@hotel.property.photoUrls</td>
                   
                    
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No hotels found. Please modify your search criteria.</p>
}
